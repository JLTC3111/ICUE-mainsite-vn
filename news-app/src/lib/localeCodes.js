/** UI + machine-translation locale codes (keep in sync with src/lib/i18n.js labels). */
export const LOCALE_CODES = ['vi', 'en', 'de', 'fr', 'ko', 'ja']

export const LOCALE_CODE_SET = new Set(LOCALE_CODES)

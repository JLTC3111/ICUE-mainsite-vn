// Default profile picture for users without a custom avatar: the ICUE logo
// (copied from the main site's favicon.png). BASE_URL keeps it correct under
// the /newsroom/ base in both dev and production.
export const DEFAULT_AVATAR = `${import.meta.env.BASE_URL}default-avatar.png`
